import express from "express";

const router = express.Router();

router.post("/post", postTask);
router.post("/post", getTask);
router.post("/post", postTask);
router.post("/post", postTask);

export default router;
